spring:
  application.name: controlmercaderia

  datasource:
    url: jdbc:mysql://localhost:3306/electric_manager?allowPublicKeyRetrieval=true&useSSL=false&useTimezone=true&serverTimezone=America/Lima&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
